= form_for @card do |f|
  -if @card.errors.any?
    #error_explanation
      %h2= "#{pluralize(@card.errors.count, "error")} prohibited this card from being saved:"
      %ul
        - @card.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.text_field :word, :placeholder => "단어를 입력하세요"
  .field
    = f.text_area :mean, :rows => 6, :placeholder => "뜻을 입력하세요"
  .actions
    = f.submit 'Save'
